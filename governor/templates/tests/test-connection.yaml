apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "governor.fullname" . }}-test-connection"
  labels:
    {{- include "governor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "governor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
