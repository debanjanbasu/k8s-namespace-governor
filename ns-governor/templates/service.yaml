apiVersion: v1
kind: Service
metadata:
  name: {{ include "ns-governor.fullname" . }}
  labels:
    {{- include "ns-governor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ns-governor.selectorLabels" . | nindent 4 }}
