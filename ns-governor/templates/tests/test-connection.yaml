apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ns-governor.fullname" . }}-test-connection"
  labels:
    {{- include "ns-governor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ns-governor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
