apiVersion: v1
kind: Pod
metadata:
  name: test-pod
  namespace: {{ .Values.rootTenantNamespace }}
  labels:
    app: test-pod
spec:
  containers:
    - name: test-container
      image: scratch
      resources:
        # limits:
        #   memory: 1m
        # requests:
        #   cpu: 1m
        #   memory: 1m
  restartPolicy: Never